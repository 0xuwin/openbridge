# OpenBridge Configuration
# Copy this file to .env and adjust values as needed.

# Server host and port
OPENBRIDGE_HOST=127.0.0.1
OPENBRIDGE_PORT=8899

# OAuth callback port (used during login flow)
OPENBRIDGE_OAUTH_PORT=1455

# Data directory (defaults to ~/.openbridge)
# OPENBRIDGE_DATA_DIR=~/.openbridge
